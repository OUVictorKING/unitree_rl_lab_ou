name: isaaclab
channels:
  - conda-forge
dependencies:
  # Isaac Sim 5.x / Isaac Lab on this machine uses Python 3.11.
  - python=3.11
  - pip
  - git
  - git-lfs
  - cmake
  - ninja
  - pip:
      - -r requirements.txt
