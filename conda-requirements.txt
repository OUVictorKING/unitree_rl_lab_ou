# Lightweight conda packages for updating the existing "isaaclab" environment.
#
# Use with:
#   conda install -n isaaclab -c conda-forge --file conda-requirements.txt
#
# Do not use this file to recreate Isaac Lab itself. Keep Isaac Sim, Isaac Lab,
# PyTorch, and CUDA packages managed by the Isaac Lab installation.

pip
git
git-lfs
cmake
ninja
